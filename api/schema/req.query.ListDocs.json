{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "$ref": "./util/limit.json"
        },
        "filter": {
            "type": "string",
            "description": "Filter docs by name or prefix"
        },
        "page": {
            "$ref": "./util/page.json"
        },
        "order": {
            "$ref": "./util/order.json"
        },
        "sort": {
            "type": "string",
            "default": "created",
            "enum": [
                "id",
                "name",
                "created",
                "updated"
            ],
            "description": "Field to sort order by"
        }
    }
}
